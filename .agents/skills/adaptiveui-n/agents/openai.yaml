interface:
  display_name: "AdaptiveUI-N"
  short_description: "Derived adaptive UI changes with required scoped audit"
  brand_color: "#2563EB"
  default_prompt: "Use $adaptiveui-n on this feature. Implement only its adaptive-UI effects, then run the required scoped style audit."
policy:
  allow_implicit_invocation: false
